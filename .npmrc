# Hoisted layout matches npm-style node_modules so Next output file tracing does not
# list the same native binaries + model chunks under both .pnpm/* and symlink duplicates
# (which was ~245MB unzipped for /api/images — over Vercel’s 250MB limit).
node-linker=hoisted
